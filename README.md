# Doksh_app

A minimal Flutter app scaffold created by the assistant.

Quick start

1. Install Flutter SDK: https://flutter.dev/docs/get-started/install
2. From the project root, run:

```powershell
flutter pub get
flutter run
```

To generate full platform folders (android/ios/web), run `flutter create .` in the project root after installing Flutter.
